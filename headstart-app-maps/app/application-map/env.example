# Airtable Personal Access Token.
# Scopes: data.records:read, data.records:write, schema.bases:read
# Granted on base app2N1SillR5AqtSC
# Both names are the same token — see README.
AIRTABLE_API_KEY=
AIRTABLE_TOKEN=

# Astute Headstart base
AIRTABLE_BASE_ID=app2N1SillR5AqtSC

# Application Map Requests
AIRTABLE_TABLE_ID=tbltYrKYfGVkWwdR1

# Manufacturers
AIRTABLE_MANUFACTURERS_TABLE_ID=tblPus2aWrpNy5pwB

# Optional: set both to turn on a shared-login gate (HTTP Basic Auth) in front
# of the whole site. Leave both blank to keep the site open. See README.
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
